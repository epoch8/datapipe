apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datapipe.fullname" . }}-test-connection"
  labels:
    {{- include "datapipe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datapipe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
