version: '3'

services:
  db:
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password

  label-studio:
    image: heartexlabs/label-studio:1.0.1
    ports:
        - 8080:8080

  redis:
    image: redis:7
    ports:
      - 6379:6379
