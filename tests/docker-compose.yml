version: '3'

services:
  db:
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password

  label-studio:
    image: heartexlabs/label-studio:1.3.0
    ports:
        - 8080:8080
