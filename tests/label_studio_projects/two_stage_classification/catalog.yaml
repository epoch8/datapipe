input_bboxes:
    type: DataTable
    columns:
        data: string

stage1_tasks:
    type: DataTable
    columns:
        data: string

stage1_annotation:
    type: DataTable
    columns:
        data: json

stage1_annotation_parsed:
    type: DataTable
    columns:
        data: json

stage1_annotation_parsed_good:
    type: DataTable
    columns:
        data: json

stage1_annotation_parsed_bad:
    type: DataTable
    columns:
        data: json

stage2_tasks:
    type: DataTable
    columns:
        data: string

stage2_annotation:
    type: DataTable
    columns:
        data: json

stage2_annotation_parsed:
    type: DataTable
    columns:
        data: json

total_annotation:
    type: DataTable
    columns:
        data: json
